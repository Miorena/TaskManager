services:
  backend:
    build: ./tasks-api
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
  
  frontend:
    build: ./tasks-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend